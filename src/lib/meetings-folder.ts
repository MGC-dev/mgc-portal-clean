/**
 * Name of the subfolder created inside each client's WorkDrive root to hold
 * their meeting summaries. Shared so the webhook that writes it and the portal
 * routes that read it can never drift apart.
 */
export const MEETINGS_FOLDER_NAME = "Meetings";
